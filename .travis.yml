language: ruby
script:
  - git config --global user.email "you@example.com"
  - git config --global user.name "Your Name"
  - bundle exec rake
rvm:
  - 2.2
  - 2.1
  - 2.0
notifications:
  - on_success: never
  - on_failure: always
